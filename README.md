# Description

Java starter project with a simple HTTP server.

# How to run

Run it from vscode or use the script to compile and run:

```bash
./run.sh
```

The server will be accessible at http://localhost:8080
